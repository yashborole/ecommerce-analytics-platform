# TODO

- [ ] Inspect `app/main.py` and define the exact “connect with file” behavior.
- [ ] Implement FastAPI app in `app/main.py` (fix `FastAPI()` and add routes).
- [ ] If needed, add an endpoint to read/write files under `data/`.
- [ ] Run the app locally and test the endpoint.

